spring:
  application:
    name: naca-cloud-native-test

server:
  port: 0

logging:
  level:
    root: WARN
    com.publicitas.naca.cloudnative: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health
